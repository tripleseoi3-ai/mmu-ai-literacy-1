---
name: interview-goal-sdd-tdd
description: 인터뷰 → 골(Goal) → SDD(명세) → TDD(테스트 주도 구현) 4단계 전체를 순서대로 진행하는 오케스트레이터. 각 단계는 전담 에이전트(interviewer / goal-setter / spec-writer / tdd-implementer)가 맡고, 단계마다 산출물 파일 하나와 사용자 승인 게이트를 남긴다. Use when starting a new feature/project from scratch and running the whole pipeline end to end, or when the user says 처음부터 제대로 하자, 인터뷰부터 시작, 전체 워크플로, SDD로 진행하자.
---

# 인터뷰 → 골 → SDD → TDD (오케스트레이터)

요구사항이 말로만 오간 뒤 곧장 코드로 넘어가는 것을 막기 위한 워크플로다.
네 단계를 **순서대로** 진행하고, 각 단계는 **파일 하나**와 **사용자 승인**을 남긴다.

**당신은 직접 작업하지 않는다. 단계별 스킬을 열고, 그 스킬이 지정한 전담 에이전트에 위임하고, 사용자 승인을 받는 것이 당신의 일이다.**

## 단계와 담당

| 단계 | 스킬 | 전담 에이전트 | 산출물 | 게이트 |
|---|---|---|---|---|
| 1 | `interview` | `interviewer` | `01-interview.md` | 종료 조건 충족 확인 |
| 2 | `goal` | `goal-setter` | `02-goal.md` | **승인 필요** |
| 3 | `sdd` | `spec-writer` | `03-spec.md` | **승인 필요 — 여기까지 승인 전 구현 금지** |
| 4 | `tdd` | `tdd-implementer` | 코드 + `04-traceability.md` | REQ마다 결과 검증 |

각 단계에 들어갈 때 해당 스킬을 Skill 도구로 연다. 세부 절차·호출 프롬프트 형식은 그 스킬 안에 있다.

## 산출물 위치

```
docs/specs/<YYYY-MM-DD>-<슬러그>/
  01-interview.md      인터뷰 원문과 요약
  02-goal.md           골, 성공 지표, 비목표, 완료 조건
  03-spec.md           SDD 본문 (REQ-001 …)
  04-traceability.md   REQ ↔ 테스트 ↔ 커밋 대응표
```

슬러그는 영문 kebab-case (예: `2026-09-05-attendance-export`). 디렉터리는 1단계 시작할 때 만든다.

## 시작 지점 확인

먼저 `docs/specs/` 를 훑어 **이미 진행 중인 건이 있는지** 본다.
- 있으면: 어디까지 됐는지 알리고 그 다음 단계부터 이어간다.
- 없으면: 1단계부터.
- 사용자가 "SDD부터" 처럼 특정 단계를 지목하면, 앞 단계 산출물을 사용자에게 받아 파일로 남긴 뒤 그 단계로 간다.

## 핵심 규칙 (전 단계 공통)

1. **단계를 건너뛰지 않는다.** 사용자가 "바로 코드 짜줘"라고 해도 최소한 골 한 줄과 완료 조건은 확정하고 넘어간다.
2. **승인 없이 다음 단계로 가지 않는다.** 승인은 사용자만 한다 — 에이전트가 "완료"라고 보고한 것은 승인이 아니다.
3. **모든 요구사항에는 ID가 붙는다.** `REQ-001` 형식. 이 ID가 명세 → 테스트 → 커밋까지 따라간다.
4. **추적성이 끊기면 미완성이다.** 테스트에 매핑되지 않은 REQ, REQ에 매핑되지 않은 테스트는 둘 다 결함이다.
5. **모른다고 말한다.** 사용자가 답하지 않은 것을 추측으로 채우지 않는다. `미결정` 에 남긴다.

## 위임 규칙

- **문서 작성·수정은 에이전트가 한다.** 사용자가 고쳐달라고 하면 같은 에이전트를 다시 호출한다. 당신이 파일을 직접 손대면 스킬 규칙이 우회된다.
- **사용자와의 대화는 당신이 한다.** 서브에이전트는 사용자에게 물을 수 없다. 에이전트가 만든 질문을 그대로 전달하고, 답변 원문을 그대로 되돌려준다.
- **보고를 그대로 믿지 않는다.** 특히 4단계에서 테스트 통과 주장은 실제 출력이 붙어 있는지 확인한다. 없으면 직접 돌린다.
- **병렬은 4단계에서만.** 건드리는 파일이 겹치지 않는 REQ끼리만 동시에 띄운다. 1~3단계는 앞 단계 산출물이 입력이므로 순차다.

## 마무리

전 REQ가 GREEN이 되면:
1. 전체 테스트를 한 번 더 돌려 결과를 그대로 보고한다 (실패가 있으면 숨기지 않는다).
2. `02-goal.md` 의 DoD 체크박스를 실제 확인한 것만 체크한다.
3. 추적성 표에 빈칸이 있으면 남은 항목을 명시한다.
4. 미결정 사항이 남아 있으면 목록으로 다시 보여준다.

## 하지 말 것

- 인터뷰 없이 요구사항을 상상해서 채우기
- 3단계 승인 전에 구현 파일 만들기
- REQ 없이 "그 김에" 기능 추가하기
- 테스트를 나중에 몰아서 쓰기
- DoD를 확인하지 않고 체크하기
- 단계 스킬을 열지 않고 당신이 직접 4단계를 다 해버리기
